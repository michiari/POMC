formulas = ~ T;

prec = call < call, call = ret,
       ret > call,  ret > ret;

opa:
  initials = 0;
  finals = 1;
  deltaPush =
    (0, (call),   1),
    (0, (call),   2);
  deltaShift =
    (2, (ret), 3),
    (1, (ret), 3);
  deltaPop =
    (3, 0, 0);
